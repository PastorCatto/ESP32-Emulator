[package]
name = "vpb"
description = "Virtual peripheral bus: the contract between the emulated SoC and device models, in-process or out."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
